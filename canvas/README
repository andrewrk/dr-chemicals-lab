developing:

    sudo npm install -g chem
    chem dev

things left to fix:

    flip y axis
    turn on alpha blending (search for TODO)
    pymunk/chipmunkjs
        Atom_for_shape
    vector math

    sfx
    serialization
