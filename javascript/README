developing:

    sudo npm install -g chem
    chem dev

things left to fix:

    claw rope is drawn at wrong place
    claw can't attach to atoms
    man jumping animation looping
    loop bond too short
    bomb explosino in wrong place
    use integer coords
    bomb explosions in the wrong spot
    animations starting at the wrong frame

    flip y axis
    turn on alpha blending (search for TODO)
    pymunk/chipmunkjs
        Atom_for_shape
    vector math

    sfx
    serialization
